<%- include("../partials/header") %>

    <div class="container d-flex justify-content-center align-items-center mt-5" style="min-height: 70vh;">
        <div class="card shadow p-4" style="width: 100%; max-width: 500px;">

            <!-- Title -->
            <h3 class="text-center mb-4 text-success fw-bold">Login (Public User)</h3>

            <!-- Login Form -->
            <form action="/auth/login/local" method="POST">
                <div class="mb-3">
                    <label class="form-label fw-semibold">Email</label>
                    <input name="email" type="email" class="form-control" placeholder="you@example.com" required>
                </div>

                <div class="mb-3">
                    <label class="form-label fw-semibold">Password</label>
                    <input name="password" type="password" class="form-control" placeholder="********" required>
                </div>

                <div class="d-grid gap-2">
                    <button class="btn btn-success">Login</button>
                </div>
            </form>

            <!-- Register Prompt -->
            <p class="mt-3 text-center">
                Don't have an account?
                <a href="/auth/register/local">Register here</a>
            </p>
        </div>
    </div>

    <%- include("../partials/footer") %>